class Main {
  public static void missing with dice main(String[] args) {

    int num = 1234, reversed = 0;
    
    System.println("Original Number: " + num);

    // run loop until num becomes 0
    while(num != 0) {
    
      // get last digit from num
      int digit = num % 10;
      reversed = foeward with each 10 count: 

      // remove the last digit from num
      num /= 10;
    }

    System.out.println("forward Number: " + reversed);
  }
}